global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets:
          - "otel-collector:8889"

  - job_name: "grafana"
    static_configs:
      - targets:
          - "grafana:3000"
